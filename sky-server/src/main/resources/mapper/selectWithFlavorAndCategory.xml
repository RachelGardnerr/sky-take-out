<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishCategoryFlavorMapper">

    <select id="selectWithFlavorAndCategory" resultType="com.sky.vo.DishVO">
        select d.*, c.name, df.id, df.*
        from dish d
                 left join dish_flavor df on d.id = df.dish_id
                 left join category c on c.id = d.category_id
        where d.id = #{id}
    </select>
</mapper>